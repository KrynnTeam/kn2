<UserControl x:Class="ShadowCheat.Controls.TriggerMenuControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <ScrollViewer x:Name="TriggerMenu" VerticalScrollBarVisibility="Hidden" PanningMode="VerticalOnly"
                  Background="Transparent" Padding="12,12,12,12">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <StackPanel x:Name="TSP1" Grid.Column="0" Margin="6,0,10,0">
                <StackPanel x:Name="TriggerBotPanel" />
                <StackPanel x:Name="AimKeyPanel" />
            </StackPanel>
            <StackPanel x:Name="TSP2" Grid.Column="1" Margin="10,0,6,0">
                <StackPanel x:Name="RapidFirePanel" />
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
